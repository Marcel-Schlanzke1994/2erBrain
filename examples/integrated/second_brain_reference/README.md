# Integrated Second Brain Reference

Defines the JSON event contract shared by Python ingestion, C parsers, Lua rules, and C++ gameplay components. Smoke validation is performed by `scripts/run_all_checks.py` through Python JSON loading and repository checks.
